#Optional rewrites
#For higher performance, move this to your http conf so this file doesn't get stat'd on every request.

#Allow cross-origin requests
#Header add Access-Control-Allow-Origin "*"
#Header add Access-Control-Allow-Headers "origin, x-requested-with, content-type"
#Header add Access-Control-Allow-Methods "PUT, GET, POST, DELETE, OPTIONS"
#Header add Content-Type "application/javascript"

#Get rid of .json extension on request.
#RewriteEngine on
#RewriteRule "^(\d+)$" "$1.json"#